## 摄像头

```
#磁盘 U盘，eMMC，SD卡这类存储硬件
lsblk
```

> List Block Devices

```
#列出PCI总线设备
lspci
#列出usb设备
lsusb
#列出内核驱动模块
lsmod
```



## 音频

## 编码器

## 灯带

四个灯节点

进入文件目录

```Plain
cd /sys/class/leds
```

进入文件

```Plain
cd led1
```

点亮

```Plain
echo 1 > brightness
```



## 按键

按键：32 48 46 30

## 指纹

```
apt update
apt install fprintd libpam-fprintd
```



## RTC

```
hwclock -w -f /dev/rtc0
hwclock -r -f /dev/rtc0 
```

## 蓝牙

```
lsusb
#输出
Bus 002 Device 001: ID 1d6b:0003 Linux Foundation 3.0 root hub
Bus 001 Device 001: ID 1d6b:0002 Linux Foundation 2.0 root hub
```



## WiFi

```
killall wpa_supplicant dhclient
ip addr flush dev wlan0
systemctl restart NetworkManager
nmcli dev wifi connect "MYZR-WiFi5" password "Myzr2012"
```



```python
#生成WiFi密码的加密配置，写入配置文件
wpa_passphrase LZJ 88888888 > /etc/wpa_supplicant.conf

wpa_passphrase MYZR-WiFi5 Myzr2012 > /etc/wpa_supplicant.conf
#启动WiFi连接
wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant.conf
#向路由器请求分配IP地址
dhclient -v wlan0
#或者
udhcpc -i wlan0
#测试联网
ping -I wlan0 8.8.8.8
ip addr show wlan0
```

```
#wifi直连
nmcli dev wifi connect "MYZR-WiFi5" password "Myzr2012"
```

Maskrom用于更新update

loader设备用于更新boot.img,0x8000

## 英文释义

| 英文                | 中文             |
| ------------------- | ---------------- |
| hub                 | 集线器、中枢节点 |
| interval            | 间隔             |
| configured          | 已配置           |
| enroll-stage-passed | 注册阶段已通过   |
| enroll-retry-scan   | 重新尝试注册扫描 |
| block               | 块设备           |
| Module              | 模块             |
| flush               | 冲洗             |
|                     |                  |

