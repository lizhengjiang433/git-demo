进入8MP容器：

```
lxc exec myzr-u2004-8MP --user 1007 --group 1007 --env HOME=/home/lizj --cwd /home/lizj -- /bin/bash
```



编译手册
========

准备编译环境
------------

1. 安装依赖包

   ```
   sudo apt install libssh-dev
   ```

1. 安装 gcc 交叉编译工具

   工具链网盘下载位置：[/](http://file.myzr.lan:8080/)[REL-发布区](http://file.myzr.lan:8080/REL-发布区/)/[MYZR-iMX8MP](http://file.myzr.lan:8080/REL-发布区/MYZR-iMX8MP/)/[3.软件资料](http://file.myzr.lan:8080/REL-发布区/MYZR-iMX8MP/3.软件资料/)/[3.3-工具](http://file.myzr.lan:8080/REL-发布区/MYZR-iMX8MP/3.软件资料/3.3-工具/)/arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu.tar.xz

   ```
   #创建安装目录
   mkdir -p ${HOME}/my-work/gcc
   
   # 安装指令
   tar -xf arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu.tar.xz -C ${HOME}/my-work/gcc/
   ```
   
   ```
   # 创建交叉编译工具配置文件
   cat <<'EOF'>> ~/my-work/gcc/arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu.env
   #!/bin/sh
   export PATH=${HOME}/my-work/gcc/arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu/bin:${PATH}
   export ARCH=arm64
   export CROSS_COMPILE=aarch64-none-linux-gnu-
   EOF
   # 配置可执行权限
   chmod +x ~/my-work/gcc/arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu.env
   ```
   
   > 定义 aarch64 交叉编译工具链环境变量，编译内核、uboot 时 source 这个文件即可切换交叉编译环境。
   
   ```
   # 加载环境
   source ~/my-work/gcc/arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu.env
   # 查看交叉编译器版本、编译环境配置
   aarch64-none-linux-gnu-gcc -v
   ```

## 安装交叉编译工具

### 安装 QT 交叉编译工具

1. 创建安装目录

   ```
   mkdir ~/my-work/linux/imx-linux-6.6.52/toolchain
   ```

2. 准备交叉编译工具安装文件

   到网盘路径 `3.软件资料/imx-linux-6.6.52/toolchain` 下载 `imx93-wayland-full-toolchain-6.6.52.sh` 到开发环境的安装目录。

3. 执行安装命令

   ```
   # 进入安装目录
   cd ~/my-work/linux/imx-linux-6.6.52/toolchain
   # 添加文件权限
   chmod +x imx93-wayland-full-toolchain-6.6.52.sh
   # 执行安装命令
   ./imx93-wayland-full-toolchain-6.6.52.sh
   ```

4. 选择安装路径

   在提示 `Enter target directory for SDK (default: /opt/fsl-imx-wayland/6.6-scarthgap):` 时，输入 `.` 表示安装到当前目录：

   ```
   NXP i.MX Release Distro SDK installer version 6.6-scarthgap
   ===========================================================
   Enter target directory for SDK (default: /opt/fsl-imx-wayland/6.6-scarthgap): .
   ```

   在提示确认安装路径时输入 `y` 表示确认：

   ```
   You are about to install the SDK to "/home/tangbin/my-work/imx-linux-6.6.52/toolchain". Proceed [Y/n]? y
   ```

5. 等待安装完成

   安装需要的时间与开发主机的性能有关系，在安装过程中，终端会持续输出，如下：

   ```
   Extracting SDK..................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ................................................................................
   ....................done
   Setting it up...done
   SDK has been successfully set up and is ready to be used.
   Each time you wish to use the SDK in a new shell session, you need to source the environment setup script e.g.
    $ . /home/tangbin/my-work/imx-linux-6.6.52/toolchain/environment-setup-armv8a-poky-linux
   ```

## 编译Linux内核

1. 准备Linux源码包“imx-linux-6.6.52”

   ```
   git clone http://myzr-git.lan:3000/linux/imx-linux-6.6.52
   ```

   

2. 配置编译工具环境变量 

   ```
   source ~/my-work/gcc/arm-gnu-toolchain-13.3.rel1-x86_64-aarch64-none-linux-gnu.env
   ```

3. Linux Image 文件编译

   ```
   # 加载内核默认配置，生成顶层编译配置文件
   make imx_v8_defconfig
   # 编译ARM64内核主镜像Image
   make Image -j24
   ```

   > 在内核顶层目录imx-linux-6.6.52执行

4. Linux 设备树文件编译

   ```
   # 单独编译自定义设备树
   make myzr/myzr-imx8mp-ek314.dtb -j$(nproc)
   # 编译内核镜像 Image + 指定 dtb
   make Image myzr/myzr-imx8mp-ek314.dtb -j$(nproc)
   ```

   
